* 统计各自负责功能的交易名和接口名。
  * 如：交易名:aa.json 接口：fn47 （无接口调用时，标注无）
* 检查各自页面的字段是否显示正常，身份证信息需要屏蔽保留前后2位，卡号保持前后4位。
* 不要自己增加报错或者提示信息。如果交易有报错，应取后台报错信息。
* 有输入框的页面，需要各种输入字段验证，如：邮箱，手机号，金额。具体参照其他页面。
* 页面信息确定。需要对比手机银行或者个人网银来确定字段取值是否确定。
* 页面切换，能不用标识字段的尽量不要使用，如：service.setSessionData('A',...),sessionStorage.setItem('A',...)
* 争取周3下班前可提交内部测试。
  
